#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_jshint@2.9.5@jshint/bin/jshint" "$@"
  ret=$?
else 
  node  "$basedir/../_jshint@2.9.5@jshint/bin/jshint" "$@"
  ret=$?
fi
exit $ret
